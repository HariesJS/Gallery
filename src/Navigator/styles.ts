import { Colors } from "../shared/lib/theme/colors";

export const headerStyle = {
    headerStyle: {
        backgroundColor: Colors.darkGray,
    }, 
    headerTitleStyle: { 
        color: Colors.white,
    }
}